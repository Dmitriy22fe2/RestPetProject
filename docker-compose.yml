version: '3.8'

services:
  test-postgres:
    image: postgres:16
    container_name: rest_pet_project-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: rest_pet_project_db
    ports:
      - "5492:5432"